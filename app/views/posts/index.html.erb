<h1>投稿一覧画面</h1>
<div class="post-item">
  <% @posts.each do |post| %>
  <div class="posts-index-item">
    <%= post.content %><br>
    <%= post.link %><br>
    <% if post.img %>
      <img src="<%= "/tweet_images/#{post.img}" %>" >
    <% end %>
    <br>
    <!-- 投稿のユーザIDと現在ログイン中のユーザIDが一致していれば詳細ボタンを表示する ,to_sで値のデータを文字列として扱う -->
    <% if post.user_id.to_s == @user.id.to_s %>
      <%= link_to("詳細", "/posts/#{post.id}") %>
    <% end %>
  <% end %>
  </div>
</div>